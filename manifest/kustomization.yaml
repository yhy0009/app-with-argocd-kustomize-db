resources:
  - argocd-img-deployment.yaml
  - argocd-img-service.yaml
  - sealed-secret.yaml
images:
  - name: nginx 
    newName: ${{ secrets.DOCKERHUB_USERNAME }}/${{ secrets.DOCKERHUB_REPOSITORY }}
    newTag: "002385c"
